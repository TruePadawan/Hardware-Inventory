extends layout

block css
    link(rel='stylesheet' href='/stylesheets/hardware-details.css')
block content
    main.flex-grow-1.mt-2
        .hardware
            if hardware.img_url !== undefined
                img.hardware-image(src=hardware.img_url alt='hardware image')
            .flex-grow-1.d-flex.flex-column
                h3.m-0!=hardware.name
                a.hardware-type.fs-5(href=hardware.hardware_type.route_url)!=hardware.hardware_type.name
                span.fs-5.price!=`${hardware.price_usd} USD`
                span.fs-6!=`${hardware.number_in_stock} available`
                p.fs-6.hardware-desc!=hardware.desc
                .d-flex.gap-1.buttons
                    a.my-button.purple.flex-grow-1(href=`${hardware.route_url}/edit`) Edit
                    a.my-button.red.flex-grow-1(href=`${hardware.route_url}/delete`) Delete